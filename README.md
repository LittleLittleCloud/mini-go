# mini-golang

## Build instructions:
./clean; ./build

## Pretty printer:
./pp [file.go]

## Clean before push
./clean

Note: if no filename is provided as argument, input is read from stdin (use Ctrl-D to terminate)

